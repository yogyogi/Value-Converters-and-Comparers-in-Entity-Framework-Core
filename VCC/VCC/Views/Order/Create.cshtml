@{
    ViewData["Title"] = "Create Order";
}

@model Order

<h1 class="bg-info text-white">Create Order</h1>
<a asp-action="Index" class="btn btn-secondary">Back</a>

<form method="post">
    <div class="form-group">
        <label asp-for="Price.Amount"></label>
        <input asp-for="Price.Amount" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Price.Currency"></label>
        <select asp-for="Price.Currency" class="form-control" asp-items="@new SelectList(Enum.GetNames(typeof(Currency)))">
        </select>
    </div>
    @* <div class="form-group">
        <label asp-for="Currency"></label>
        <select asp-for="Currency" class="form-control" asp-items="@new SelectList(Enum.GetNames(typeof(Currency)))">
        </select>
    </div> *@
    <button type="submit" class="btn btn-primary">Create</button>
</form>